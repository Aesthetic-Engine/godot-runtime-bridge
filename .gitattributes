# Normalize line endings for all files that Git considers text files.
* text=auto eol=lf

# GitHub source archives intentionally contain the full GRB product surface.
# Addon-only distribution is built explicitly by the release workflow from
# addons/godot-runtime-bridge/ rather than by hiding repo files here.
